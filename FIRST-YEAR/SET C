Question 3: Sum of Digits 
#include <stdio.h> 
int main() { 
    int num, sum; 
    printf("Enter a number: "); 
    scanf("%d", num); 
    while(num != 0) { 
        sum = sum + num % 10; 
        num = num / 10; 
    } 
    printf("Sum of digits = %d", sum); 
    return 0; 
} 
