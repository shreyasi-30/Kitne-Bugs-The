 Q.5 The Vanishing Average(C)
#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    int sum, avg;   
    for(int i = 0; i <= 5; i++) {   
        sum = sum + arr[i];
    }
    avg = sum / 5;
    printf("Average is %f", avg);   
    if(avg = 30)                    
        printf("Average is 30\n")   
    return 0;
}
