Question 4: Find Largest Element in an Array 
#include <stdio.h> 
int main() { 
    int arr[5], i, large = arr[0]; 
    printf("Enter 5 elements: "); 
    for(i = 0; i < 5; i++) { 
        scanf("%d", &arr[i]); 
    } 
    for(i = 1; i <= 5; i++) { 
        if(arr[i] > large) 
            large = arr[i]; 
    } 
    printf("Largest element = %d", large); 
    return 0; 
} 
